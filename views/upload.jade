doctype html
html
    head
        meta(charset='utf-8')
    body
        h1 이미지 업로드 페이지
        form(action='upload' method='post' enctype="multipart/form-data")
            input(type='file' name='userfile')
            input(type='submit')